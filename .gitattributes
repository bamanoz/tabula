* text=auto

# Unix entrypoints must stay executable from Linux containers and WSL even
# when the checkout is created by Git for Windows with core.autocrlf=true.
*.sh text eol=lf
Makefile text eol=lf
