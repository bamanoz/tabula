[pytest]
python_files = test_*.py
testpaths = tests skills
norecursedirs = .git .venv
addopts = -ra
markers =
    unit: fast logic-only coverage
    smoke: lightweight runtime boot/connect checks
    e2e: heavier runtime and integration flows
    contract: protocol and extension contract checks
    manual: helper or real-env test not part of CI matrix
