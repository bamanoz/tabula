# .opencode/.gitignore — Normalized for plugin build chain reproducibility
# See: memory-bank/tasks.md (A1e) — versioned config files must be tracked

# Runtime dependencies
node_modules/

# Build artifacts (compiled plugin output)
dist/

# Lockfiles (regenerated by npm install)
package-lock.json
bun.lock
