{
  "id": "memory",
  "config": {
    "entries": [
      {
        "key": "embedding.api_key",
        "type": "string",
        "secret": true,
        "default": "",
        "global_key": "memory.embedding.api_key",
        "env": "TABULA_SKILL_MEMORY_EMBEDDING_API_KEY",
        "env_aliases": [
          "TABULA_EMBEDDING_API_KEY",
          "OPENAI_API_KEY"
        ],
        "store_ids": [
          "memory.embedding_api_key",
          "driver-openai.api_key"
        ]
      },
      {
        "key": "embedding.model",
        "type": "string",
        "default": "text-embedding-3-small",
        "global_key": "memory.embedding.model",
        "env": "TABULA_SKILL_MEMORY_EMBEDDING_MODEL",
        "env_aliases": [
          "TABULA_EMBEDDING_MODEL"
        ]
      },
      {
        "key": "embedding.base_url",
        "type": "string",
        "default": "https://api.openai.com",
        "global_key": "memory.embedding.base_url",
        "env": "TABULA_SKILL_MEMORY_EMBEDDING_BASE_URL",
        "env_aliases": [
          "OPENAI_BASE_URL"
        ]
      }
    ]
  }
}
