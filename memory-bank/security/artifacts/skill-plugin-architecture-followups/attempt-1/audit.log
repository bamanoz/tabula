# Dependency Audit Log — skill-plugin-architecture-followups — Attempt 1

Date: 2026-04-27

## Dependency manifest delta

Command:

```sh
git diff -- package.json package-lock.json requirements.txt pyproject.toml go.mod go.sum skills/_tslib/package.json skills/_tslib/bun.lock tools/tabula-distro/pyproject.toml examples/plugin-sdk-python/pyproject.toml .opencode/package.json .opencode/package-lock.json
```

Output: no output. BUILD introduced no dependency manifest or lockfile changes.

## npm audit

Command run in `.opencode/`:

```sh
npm audit --json
```

Summary:

```json
{
  "auditReportVersion": 2,
  "metadata": {
    "vulnerabilities": {
      "info": 0,
      "low": 0,
      "moderate": 3,
      "high": 0,
      "critical": 0,
      "total": 3
    },
    "dependencies": {
      "prod": 22,
      "dev": 0,
      "optional": 9,
      "peer": 0,
      "peerOptional": 0,
      "total": 30
    }
  },
  "vulnerabilities": {
    "@opencode-ai/plugin": {
      "severity": "moderate",
      "isDirect": true,
      "via": ["effect"],
      "fixAvailable": {"name": "@opencode-ai/plugin", "version": "1.4.3", "isSemVerMajor": true}
    },
    "effect": {
      "severity": "moderate",
      "isDirect": false,
      "via": ["uuid"],
      "effects": ["@opencode-ai/plugin"]
    },
    "uuid": {
      "severity": "moderate",
      "isDirect": false,
      "via": ["GHSA-w5hq-g745-h8pq: Missing buffer bounds check in v3/v5/v6 when buf is provided"],
      "range": "<14.0.0"
    }
  }
}
```

Security interpretation: WARNING. The audit finding is in `.opencode` plugin tooling dependencies and no BUILD dependency change introduced it. It should be tracked outside this BUILD gate, but it is not a blocking finding for the audited repo-local hardening changes.

## pip-audit

Command:

```sh
pip-audit --format=json
```

Output:

```text
zsh:1: command not found: pip-audit
```

Security interpretation: WARNING (degraded). The audit tool is unavailable in this environment. No Python dependency manifests changed in BUILD.
