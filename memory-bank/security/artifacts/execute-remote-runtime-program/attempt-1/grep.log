# Grep Transcript Summary

Command executed:

```text
grep -rEn "(api[_-]?key|secret|token|password|bearer)" cmd/tabula cmd/tabula-runtime internal/runtime internal/kernel docs/issues go.mod go.sum
```

Relevant findings:

- `internal/runtime/auth/token.go` issues random `rtk_` bearer tokens, writes `$TABULA_HOME/run/runtime-token`, validates via constant-time compare, and returns sanitized unauthorized errors.
- `cmd/tabula/main.go` logs token issuance by `runtime_id` only; no token value logged.
- `cmd/tabula-runtime/dialer/dialer.go` reads token material from `token_file` before each dial and sends it only in `Hello.token`; errors include token file path, not token content.
- Tests contain fixture strings such as `secret-token`, `bad-token`, and `rtk_bad_secret`; these are test-only fixtures.
- `docs/issues/M4-08-kernel-runtime-registry-config.md` still contains stale comment examples using `token = "..."`; implementation audited in `cmd/tabula-runtime/config/config.go` rejects stale `token` and accepts only `token_file`.

No hard-coded production token/API key/password values found in audited source files.
