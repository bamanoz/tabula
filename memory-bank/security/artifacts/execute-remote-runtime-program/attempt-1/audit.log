# Dependency Audit

Go dependency delta:

- Added `github.com/coder/websocket v1.8.14` in `go.mod`/`go.sum`.

Available lockfile audit:

```text
Command: npm audit --json (workdir: /Users/mak/src/tabula/.opencode)
Result: 3 moderate vulnerabilities in the local .opencode plugin environment:
- @opencode-ai/plugin via effect
- effect via uuid
- uuid GHSA-w5hq-g745-h8pq
```

Applicability:

- These npm findings are outside the Go runtime program dependency delta and outside changed product source.
- No `package-lock.json` exists for the Go runtime program itself.

Degraded:

- No Go vulnerability audit tool (`govulncheck`) is part of the SECURITY checklist allowed commands, so the newly added Go module was reviewed by manifest/diff inspection only.
