# External Bundle Migration Matrix — skill-plugin-architecture-followups

Purpose: track external `tabula-bundles` evidence needed before this repo deletes migration bridges for the skill/plugin architecture follow-ups. PLAN seeded this artifact with blocker rows; BUILD/QA should update rows in place with real external paths, commands, and linked commits/PRs.

## Status Legend

- `unknown/blocker`: not yet verified; blocks related compatibility deletion.
- `in-progress/blocker`: migration started but not validated; blocks related compatibility deletion.
- `green`: source, target, validation, and linked evidence are complete enough to unblock dependent repo cleanup.
- `not-applicable`: explicitly justified as no longer part of product behavior.

## Required Rows

| Area | Owner / Repo | Source Path | Target Plugin/Skill Path | State | Validation Command | Linked Commit/PR | Blocks Until Green | Notes |
|---|---|---|---|---|---|---|---|---|
| Hook plugins | external bundle owner / `tabula-bundles` | `/Users/mak/src/tabula-bundles/{base/hook-permissions,base/hook-logger,base/observer,coder-workspace/hook-workspace-boundary,coder-workspace/hook-approvals,caveman/hook-caveman}` old WebSocket hook-style `SKILL.md` components | Same component paths with `plugin.toml`, `README.md`, and stdio plugin `run.py` runtime using `tabula_plugin_sdk`; old `SKILL.md` removed/replaced by `README.md` | green | `py_compile` for all migrated hook runtimes; `go test ./internal/kernel -run TestTabulaBundlesHookPluginsLiveE2E -count=1 -timeout 30s`; `go test ./internal/kernel/plugin ./internal/kernel -count=1 -timeout 90s`; `PYTHONPATH="tools/tabula-distro/src" python3 -m unittest tools/tabula-distro/tests/test_install.py`; external bundle install -> `installed generation=1 bundles=10 skills=25 plugins=6 kernel_version=0.8.0`, `plugins=[hook-approvals hook-caveman hook-logger hook-permissions hook-workspace-boundary observer]`, `lib_python=True` | local workspace changes pending external commit/PR | Deleting hook-related migration bridges or docs that assume in-repo/runtime-only hooks | Hook plugin manifests and authoritative register subscriptions are validated by kernel runtime. E2E covers security-deny (`hook-permissions`), workspace-deny, approvals rewrite, caveman session context rewrite, and logger write. Existing kernel tests cover invalid subscription rejection and malformed/unknown `event_reply` fail-closed behavior. `observer` is compile/install validated; live HTTP server smoke is not included because it binds a port and should use a dedicated free-port harness later. |
| MCP plugin | external bundle owner / `tabula-bundles` | `/Users/mak/src/tabula-bundles/base/mcp` old per-call `SKILL.md` plus boot-time `mcp.register.mcp_tool_entries` first-class discovery helper | `/Users/mak/src/tabula-bundles/base/mcp/plugin.toml`, stdio plugin `run.py`, `README.md`; generic tools register at startup and first-class `mcp__<server>__<tool>` tools publish via runtime `update_tools` | green | `py_compile base/mcp/run.py base/mcp/register.py base/mcp/daemon.py base/mcp/pool.py base/mcp/client.py`; `go test ./internal/kernel -run TestTabulaBundlesMCPPluginLiveE2E -count=1 -timeout 30s`; `go test ./internal/kernel/plugin ./internal/kernel -count=1 -timeout 90s`; distro tests OK; external bundle install -> `installed generation=1 bundles=10 skills=24 plugins=7 kernel_version=0.8.0`, `plugins=[hook-approvals hook-caveman hook-logger hook-permissions hook-workspace-boundary mcp observer]`, `lib_python=True` | local workspace changes pending external commit/PR | Claiming external plugin migration complete | MCP E2E uses a fake stdio MCP server and validates plugin lifecycle/register, generic tool calls, runtime `update_tools` first-class discovery, `mcp__fake__echo` dispatch, and legacy `run.py tool mcp_list_servers` command. Existing kernel tests cover malformed `tool_result`, invalid `update_tools`, and side-effect-free rejection of malformed inbound replies/messages. |
| Driver/subagent plugins | external bundle owner / `tabula-bundles` | `/Users/mak/src/tabula-bundles/coder-subagents/subagents` old per-call `SKILL.md`; `drivers/driver` and `drivers/subagent` process runners | `/Users/mak/src/tabula-bundles/coder-subagents/subagents/plugin.toml`, stdio plugin `run.py`, plugin-local `types/*.toml`; `drivers/driver` and `drivers/subagent` remain process runners because they do not own spawn lifecycle | green for primary spawn owner | `py_compile coder-subagents/subagents/run.py drivers/subagent/run.py drivers/driver/run.py`; `go test ./internal/kernel -run TestTabulaBundlesSubagentsPluginLimitsLiveE2E -count=1 -timeout 30s`; `gofmt -w internal/kernel && go test ./internal/kernel ./internal/kernel/plugin -count=1 -timeout 90s` -> kernel/plugin cleanup green; distro tests OK; external bundle install -> `installed generation=1 bundles=10 skills=23 plugins=8 kernel_version=0.8.0`, `plugins=[hook-approvals hook-caveman hook-logger hook-permissions hook-workspace-boundary mcp observer subagents]`, `subagent_types=True` | local workspace changes pending external commit/PR | D1.11(b) kernel spawn bridge and skipped kernel spawn tests removed in repo cleanup pass; no remaining active runtime dependency on generic kernel spawn token bridge | Primary replacement auth is plugin-side ownership: subagent plugin spawns child runner processes directly and records registry depth. E2E validates lifecycle/register, spawn/list/kill surface, successful async spawn, MaxChildren denial, depth denial, parent-session list visibility, and kill cleanup. Repo cleanup removed `SpawnTokenStore`, `generateSpawnToken`, `CanSpawn`, kernel MaxChildren/depth bridge fields, kernel process spawn env injection, and legacy skipped spawn tests; `NewHub` constructor signature remains compatible but ignores spawn policy values. Existing kernel plugin tests cover invalid catalogs/malformed protocol replies fail-closed. Drivers remain runners, not tool-owning plugins. |
| Gateway plugins | external distro owner / `tabula-distrib` (not `tabula-bundles`; no gateway components exist there) | `/Users/mak/src/tabula-distrib/claw/skills/gateway-api`, `/Users/mak/src/tabula-distrib/claw/skills/gateway-telegram`; interactive gateways remain clients/user-owned launchers | `/Users/mak/src/tabula-distrib/claw/plugins/gateway-api-plugin/plugin.toml`, `/Users/mak/src/tabula-distrib/claw/plugins/gateway-telegram-plugin/plugin.toml` wrapper plugins own daemon process lifecycle and expose status tools | green for daemon gateways | `py_compile claw/plugins/gateway-api-plugin/run.py claw/plugins/gateway-telegram-plugin/run.py claw/skills/gateway-api/run.py claw/skills/gateway-telegram/run.py guardian/skills/gateway-cli/run.py`; `go test ./internal/kernel -run TestTabulaDistribGatewayPluginWrappersLiveE2E -count=1 -timeout 30s`; `go test ./internal/kernel/plugin ./internal/kernel -count=1 -timeout 90s`; distro tests OK | local workspace changes pending external commit/PR | Source plugin wrappers moved out of `skills/` into `plugins/`; interactive CLI/TUI launcher assumptions remain intentional | Gateway E2E uses fake daemon runner scripts and validates manifest load/register, plugin-side child process ownership, status tools, and shutdown cleanup. Existing kernel plugin tests cover malformed protocol replies/messages without partial state mutation. |
| Remaining per-call skills with `tools[].exec` | external bundle owner / `tabula-bundles` plus kernel/distro validator owner | `/Users/mak/src/tabula-bundles/{base/mcp,caveman/caveman-compress,coder-git/git,coder-review/review,coder-subagents/subagents,coder-tasks/todo,coder-workspace/ask-user,coder-workspace/workspace,files/files,memory/memory-admin,memory/memory-save,memory/memory-search}/SKILL.md` | Same skill paths, migrated to YAML `tools[]` entries with non-empty `name` and `exec` commands of the form `python3 skills/<skill>/run.py tool <tool>` | green | `PYTHONPATH="tools/tabula-distro/src" python3 - <<'PY' ... install all local /Users/mak/src/tabula-bundles bundles with VERSION=0.8.0 ... PY` -> `installed generation=1 bundles=10 skills=31 plugins=0 kernel_version=0.8.0` | local workspace changes pending external commit/PR | Removing legacy per-call metadata assumptions and docs references for per-call skill manifests only | Distro install validation exercised actual external bundle paths and the repo-side `tools[].name`/`tools[].exec` manifest gate. Functional smoke tests are split into a separate safe-harness follow-up because some tools are interactive, destructive, or require external services. This does not mark hook/MCP long-lived plugin migration, driver/subagent plugin migration, gateway plugin migration, or SDK package rows green. |
| Python SDK wheel | external bundle owner / `tabula-bundles` | `/Users/mak/src/tabula-bundles/_lib/python` plus old consumer imports from `skills._pylib.*` across hooks, subagents, drivers, sessions, cron, workspace, todo, pair, ask-user, and MCP daemon helpers | `/Users/mak/src/tabula-bundles/_lib/python/dist/tabula_plugin_sdk-0.1.0-py3-none-any.whl`; consumers import `tabula_plugin_sdk`, `tabula_plugin_sdk.paths`, `tabula_plugin_sdk.config`, `tabula_plugin_sdk.kernel_client`, `tabula_plugin_sdk.filelock`, and `tabula_plugin_sdk.protocol` | green | `PYTHONPATH="src" python3 -m unittest tests/test_contract.py` -> `Ran 6 tests, OK`; `python3 -m compileall -q _lib/python/src base coder-subagents coder-workspace drivers coder-tasks` -> OK; `python3 -m pip wheel . --no-deps --wheel-dir dist` -> rebuilt wheel; installed-wheel contract tests -> `Ran 6 tests, OK`; legacy import grep -> no `skills._pylib` / `skills._tslib` hits in `tabula-bundles` | local workspace changes pending external commit/PR | Removing `skills/_pylib`, `skills._pylib`, and distro `_pylib` preserve behavior is now unblocked on the external consumer side; repo-side compatibility deletion still needs its own cleanup pass and tests | Packaged SDK now carries the shared runtime helpers previously consumed from `skills._pylib`. Active bundle consumers were migrated to packaged SDK imports. No `skills._tslib` imports existed in `tabula-bundles`. |
| TypeScript SDK tarball | external bundle owner / `tabula-bundles` | `/Users/mak/src/tabula-bundles/_lib/typescript` | `/Users/mak/src/tabula-bundles/_lib/typescript/dist/tabula-skill-sdk-0.1.0.tgz` | green | `bun install`; `bun test` -> `4 pass, 0 fail`; `bun run typecheck` -> OK; `bun pm pack --destination dist` -> `tabula-skill-sdk-0.1.0.tgz`; temp consumer `bun install <tarball> && bun test` -> `2 pass, 0 fail` | local workspace changes pending external commit/PR | Removing `skills/_tslib`, `skills._tslib`, and distro `_tslib` preserve behavior after consumer migration cleanup | Tarball package name is `@tabula/skill-sdk`; artifact filename is `tabula-skill-sdk-0.1.0.tgz`. Consumer smoke imports `@tabula/skill-sdk` and `@tabula/skill-sdk/protocol`, matching current `gateway-tui` usage. |
| Bundle `_lib` install paths | external bundle owner plus kernel/distro owner | `/Users/mak/src/tabula-bundles/_lib/python` and `/Users/mak/src/tabula-bundles/_lib/typescript` sibling root plus optional per-bundle `_lib` roots | `$TABULA_HOME/_lib/python` and `$TABULA_HOME/_lib/typescript` via distro generation `_lib` runtime surface; active consumers import packaged `_lib` helpers instead of magic `skills/_*` dirs | green for package artifact install paths and active external imports | `PYTHONPATH="tools/tabula-distro/src" python3 -m unittest tools/tabula-distro/tests/test_install.py` -> `Ran 34 tests, OK`; bundle live E2E `go test ./internal/kernel -run 'TestTabulaBundles(HookPluginsLiveE2E|MCPPluginLiveE2E|SubagentsPluginLimitsLiveE2E)' -count=1 -timeout 60s` -> OK; external bundle import grep -> no `skills._pylib` / `skills._tslib` hits | local workspace changes pending external commit/PR | Removing inline sibling `_*` support-dir compatibility in `_materialize_inline_symlinks` and magic `skills/_*` support-dir handling is now unblocked on the external consumer side; repo-side compatibility deletion still needs its own cleanup pass and tests | Python and TypeScript `_lib` sibling-root install are validated for mixed skill/plugin bundles. Active external Python consumers now use `tabula_plugin_sdk.*`; TypeScript legacy imports were absent. |

## Repo-Side Cleanup Gating Summary

- `cmd/tabula/kernel.tools.json` cleanup can proceed for live metadata advertisement, but external completion must not be claimed until per-call skill/plugin rows are green or explicitly not applicable.
- Per-call `tools[].exec` migration cannot be marked green from path migration or `SKILL.md` file presence alone; repo-side validation now rejects missing/blank `name` or `exec` in boot skill descriptors and advertised `SKILL.md` `tools[]` entries, while no-tool placeholder or legacy skills remain compatible but do not count as migrated per-call skill evidence. External rows still require linked migrated manifests that pass these validators and at least one validation command/result against the actual external bundle paths.
- Long-lived plugin rows cannot be marked green from plugin path migration or process-start smoke tests alone; repo-side validation now rejects invalid dynamic `register` catalogs, invalid `update_tools` catalogs atomically, unsupported subscription events, unknown/empty `event_reply` outcomes, and ambiguous/empty `tool_result` replies. External rows still require linked validation that actual migrated plugins exercise these register/update/reply negative paths without partial dispatch-table mutation or permissive security-hook outcomes.
- D1.11(b) kernel spawn bridge deletion is complete after the driver/subagent plugin row turned green for the primary spawn owner. Validation: `gofmt -w internal/kernel && go test ./internal/kernel ./internal/kernel/plugin -count=1 -timeout 90s`.
- SDK/lib relocation deletion is complete. Repo-side cleanup removed `skills/_pylib`, `skills/_tslib`, distro `_pylib`/`_tslib` preserve behavior, and inline sibling `_*` support-dir materialization. Validation: `go test ./cmd/tabula ./internal/kernel ./internal/kernel/plugin -count=1 -timeout 120s`, `PYTHONPATH="tools/tabula-distro/src" python3 -m unittest tools/tabula-distro/tests/test_install.py`, and runtime grep/glob checks for removed SDK dirs all pass.
- Documentation cleanup must cite this artifact or linked external evidence when stating that compatibility bridges are removed. Active authoring docs may already say generic `api.spawn` / `TABULA_SPAWN_TOKEN` are not stable common contracts per CREATIVE §13; that does not imply D1.11(b) bridge deletion is unblocked.
